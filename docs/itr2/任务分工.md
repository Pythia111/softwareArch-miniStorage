# 迭代二任务分工

## ⚠️ 核心原则

1. **避免方法重复**：统一在MemFs类中实现核心逻辑,避免各成员重复实现路径解析、节点定位等方法
2. **统一接口**：所有命令都调用MemFs的私有核心方法,不自己实现
3. **及时测试**：每个成员完成自己的部分后,立即编写单元测试验证,不等集成
4. **明确优先级**：基础层→核心层→命令层,按优先级完成

---

## 架构设计

```
┌───────────────────────────────────────────────┐
│  命令层 (MemFs公共方法)                          │
│  mkdir/touch/ls/info/find/rm/link            │
│  各成员实现,调用核心方法                         │
└───────────────────────────────────────────────┘
                    ↓ 调用
┌───────────────────────────────────────────────┐
│  核心方法层 (MemFs私有方法) - 成员4提供          │
│  locateNode / getParentDir / resolveLink      │
│  ⭐ 所有人复用,避免重复实现                      │
└───────────────────────────────────────────────┘
                    ↓ 调用
┌───────────────────────────────────────────────┐
│  基础层                                        │
│  Node/File/Directory/Link (成员1)            │
│  PathUtil (成员3)                             │
└───────────────────────────────────────────────┘
```

---

## 优先级1：基础层(并行完成)

### 成员1 (sy)：Node体系 + Link类 + SizeContext

**依赖**：无
**被依赖**：成员2、成员4

#### 任务清单

1. **实现Link类**
   ```java
   public class Link extends Node {
       private Node target;

       public Link(String name, Node target) {
           super(name);
           this.target = target;
       }

       public Node getTarget() {
           return target;
       }

       @Override
       public long getSize(SizeContext ctx) {
           return target.getSize(ctx);
       }
   }
   ```

2. **升级SizeContext类** (从预留改为实际使用)
   ```java
   public class SizeContext {
       private Set<Node> visited = new HashSet<>();

       public boolean isVisited(Node node) {
           return visited.contains(node);
       }

       public void markVisited(Node node) {
           visited.add(node);
       }
   }
   ```

3. **升级Directory.getSize()** (支持防重复计数)
   ```java
   @Override
   public long getSize(SizeContext ctx) {
       long total = 0;
       for (Node child : children.values()) {
           // 获取底层节点(如果是Link则获取target)
           Node realNode = (child instanceof Link)
               ? ((Link)child).getTarget()
               : child;

           // 只计算未访问过的节点
           if (!ctx.isVisited(realNode)) {
               ctx.markVisited(realNode);
               total += child.getSize(ctx);
           }
       }
       return total;
   }
   ```

4. **为Node添加类型判断方法**
   ```java
   public abstract class Node {
       public boolean isFile() { return this instanceof File; }
       public boolean isDirectory() { return this instanceof Directory; }
       public boolean isLink() { return this instanceof Link; }
       // ...
   }
   ```

#### 测试要求
- 测试Link的创建和getTarget
- 测试SizeContext的访问标记功能
- 测试Directory.getSize在有链接共享节点时的防重复计数
- 构造测试场景：
  ```
  /data (目录,包含a和b文件)
  /alias -> /data (链接)
  验证根目录大小只计算一次/data的内容
  ```

---

### 成员3 (cyg)：PathUtil路径规范化

**依赖**：无
**被依赖**：所有人

#### 任务清单

1. **升级PathUtil.normalize()** ⭐ 最关键方法
   ```java
   public static String normalize(String absPath) {
       // 1. 检查是否绝对路径
       if (absPath == null || !absPath.startsWith("/")) {
           return null;
       }

       // 2. 分割路径
       String[] parts = absPath.split("/+");

       // 3. 使用栈处理 . 和 ..
       List<String> stack = new ArrayList<>();
       for (String part : parts) {
           if (part.isEmpty() || part.equals(".")) {
               continue;
           } else if (part.equals("..")) {
               if (!stack.isEmpty()) {
                   stack.remove(stack.size() - 1);
               }
           } else {
               stack.add(part);
           }
       }

       // 4. 重新拼接
       if (stack.isEmpty()) return "/";
       return "/" + String.join("/", stack);
   }
   ```

2. **提供辅助方法** (输入必须是已规范化的路径)
   ```java
   // 分割路径: /a/b/c -> ["a", "b", "c"]
   public static String[] split(String normalizedPath) {
       if (normalizedPath.equals("/")) return new String[0];
       return normalizedPath.substring(1).split("/");
   }

   // 获取父路径: /a/b/c -> /a/b
   public static String getParentPath(String normalizedPath) {
       if (normalizedPath.equals("/")) return null;
       int lastSlash = normalizedPath.lastIndexOf('/');
       if (lastSlash == 0) return "/";
       return normalizedPath.substring(0, lastSlash);
   }

   // 获取文件名: /a/b/c -> c
   public static String getBaseName(String normalizedPath) {
       if (normalizedPath.equals("/")) return "";
       int lastSlash = normalizedPath.lastIndexOf('/');
       return normalizedPath.substring(lastSlash + 1);
   }
   ```

#### 测试要求
- 测试路径规范化的各种情况：
  - `//usr///local` → `/usr/local`
  - `/usr/local/./a.txt` → `/usr/local/a.txt`
  - `/usr/local/../b.txt` → `/usr/b.txt`
  - `/a//b/./c/../` → `/a/b`
  - `/..` → `/`
  - `/../..` → `/`
- 测试辅助方法的正确性
- 非绝对路径返回null

---

## 优先级2：核心方法层

### 成员4 (hly)：MemFs核心框架 + Main类

**依赖**：成员1、成员3
**被依赖**：所有人

#### 任务清单

1. **实现MemFs核心私有方法** ⭐ 关键 - 所有命令都要用这些方法

   ```java
   public class MemFs {
       private Directory root = new Directory("/");

       // ===== 核心私有方法 =====

       /**
        * 根据绝对路径定位节点
        * @param absPath 原始路径(未规范化)
        * @return 找到的节点,不存在返回null
        */
       private Node locateNode(String absPath) {
           String normalized = PathUtil.normalize(absPath);
           if (normalized == null) return null;
           if (normalized.equals("/")) return root;

           String[] parts = PathUtil.split(normalized);
           Node current = root;
           for (String part : parts) {
               if (!(current instanceof Directory)) return null;
               current = ((Directory)current).getChild(part);
               if (current == null) return null;
           }
           return current;
       }

       /**
        * 获取父目录
        * @param absPath 原始路径(未规范化)
        * @return 父目录,不存在或父路径不是目录返回null
        */
       private Directory getParentDirectory(String absPath) {
           String normalized = PathUtil.normalize(absPath);
           if (normalized == null || normalized.equals("/")) return null;

           String parentPath = PathUtil.getParentPath(normalized);
           Node parent = locateNode(parentPath);
           return (parent instanceof Directory) ? (Directory)parent : null;
       }

       /**
        * 解析链接到最终节点
        * @param node 原始节点
        * @return 如果是Link返回target,否则返回自身
        */
       private Node resolveLink(Node node) {
           return (node instanceof Link) ? ((Link)node).getTarget() : node;
       }

       /**
        * 获取路径的最后一个组件
        * @param absPath 原始路径(未规范化)
        * @return 名称,根目录返回null
        */
       private String getBaseName(String absPath) {
           String normalized = PathUtil.normalize(absPath);
           if (normalized == null || normalized.equals("/")) return null;
           return PathUtil.getBaseName(normalized);
       }

       // ===== 公共方法(各成员实现) =====
       // 见优先级3
   }
   ```

2. **实现Main类**
   ```java
   public static void main(String[] args) {
       MemFs fs = new MemFs();
       Scanner scanner = new Scanner(System.in);

       while (scanner.hasNextLine()) {
           String line = scanner.nextLine().trim();
           if (line.isEmpty()) continue;

           String[] tokens = line.split("\\s+");
           String cmd = tokens[0];

           switch (cmd) {
               case "MKDIR":
                   if (tokens.length == 2) fs.mkdir(tokens[1]);
                   break;
               case "TOUCH":
                   if (tokens.length == 3) {
                       try {
                           int size = Integer.parseInt(tokens[2]);
                           if (size >= 0) fs.touch(tokens[1], size);
                       } catch (NumberFormatException e) {}
                   }
                   break;
               case "LS":
                   if (tokens.length == 2) {
                       List<String> result = fs.ls(tokens[1]);
                       for (String s : result) System.out.println(s);
                   }
                   break;
               case "INFO":
                   if (tokens.length == 2) {
                       Long size = fs.info(tokens[1]);
                       if (size != null) System.out.println(size);
                   }
                   break;
               case "FIND":
                   if (tokens.length == 3) {
                       List<String> result = fs.find(tokens[1], tokens[2]);
                       for (String s : result) System.out.println(s);
                   }
                   break;
               case "RM":
                   if (tokens.length == 2) fs.rm(tokens[1]);
                   break;
               case "LINK":
                   if (tokens.length == 3) fs.link(tokens[1], tokens[2]);
                   break;
           }
       }
   }
   ```

#### 测试要求
- 测试locateNode对各种路径的解析
- 测试getParentDirectory的正确性
- 测试resolveLink的链接跟随
- 构造测试场景验证核心方法的协作

---

## 优先级3：命令层(依赖成员4的核心方法)

### 成员1 (sy)：INFO命令

**依赖**：成员4的核心方法

#### 任务清单

在MemFs类中添加info方法：
```java
public Long info(String absPath) {
    Node node = locateNode(absPath);  // 调用核心方法
    if (node == null) return null;

    // 跟随链接
    Node target = resolveLink(node);  // 调用核心方法

    return target.getSize(new SizeContext());
}
```

#### 测试要求
- 测试文件、目录、链接的大小计算
- 测试链接共享节点不重复计数的场景

---

### 成员2 (cx)：LS命令 + FIND命令

**依赖**：成员4的核心方法

#### 任务清单

1. **实现LS命令**
   ```java
   public List<String> ls(String absPath) {
       Node node = locateNode(absPath);  // 调用核心方法
       if (node == null) return Collections.emptyList();

       // 跟随链接
       Node target = resolveLink(node);  // 调用核心方法

       if (target.isFile()) {
           // 链接到文件时,返回链接自身名称
           return Collections.singletonList(node.getName());
       } else if (target.isDirectory()) {
           // 目录或链接到目录,返回子节点列表
           return new ArrayList<>(((Directory)target).listChildren());
       }

       return Collections.emptyList();
   }
   ```

2. **实现FIND命令**
   ```java
   public List<String> find(String absPath, String targetName) {
       Node startNode = locateNode(absPath);  // 调用核心方法
       if (startNode == null) return Collections.emptyList();

       List<String> results = new ArrayList<>();
       Set<Directory> visitedDirs = new HashSet<>();

       // 规范化路径作为当前路径前缀
       String normalized = PathUtil.normalize(absPath);
       findRecursive(startNode, normalized, targetName, results, visitedDirs);

       Collections.sort(results);
       return results;
   }

   private void findRecursive(Node node, String currentPath, String targetName,
                              List<String> results, Set<Directory> visitedDirs) {
       // 检查当前节点名称是否匹配
       if (node.getName().equals(targetName)) {
           results.add(currentPath);
       }

       // 如果是目录或链接到目录,递归搜索
       Node target = resolveLink(node);  // 调用核心方法
       if (target.isDirectory()) {
           Directory dir = (Directory) target;

           // 防重复:同一底层目录只展开一次
           if (visitedDirs.contains(dir)) return;
           visitedDirs.add(dir);

           for (String childName : dir.listChildren()) {
               Node child = dir.getChild(childName);
               String childPath = currentPath.equals("/")
                   ? "/" + childName
                   : currentPath + "/" + childName;
               findRecursive(child, childPath, targetName, results, visitedDirs);
           }
       }
   }
   ```

#### 测试要求
- 测试LS对文件、目录、链接的处理
- 测试FIND的递归搜索和字典序排序
- 测试FIND的防重复展开(链接到同一目录)

---

### 成员3 (cyg)：MKDIR命令 + LINK命令

**依赖**：成员4的核心方法

#### 任务清单

1. **升级MKDIR命令** (支持覆盖语义)
   ```java
   public void mkdir(String absPath) {
       String normalized = PathUtil.normalize(absPath);
       if (normalized == null || normalized.equals("/")) return;

       Directory parent = getParentDirectory(absPath);  // 调用核心方法
       if (parent == null) return;

       String name = getBaseName(absPath);  // 调用核心方法

       // 检查是否已存在目录
       Node existing = parent.getChild(name);
       if (existing != null && existing.isDirectory()) {
           return;  // 已存在目录,保持不变
       }

       // 创建新目录(TreeMap会自动覆盖)
       parent.putChild(name, new Directory(name));
   }
   ```

2. **实现LINK命令**
   ```java
   public void link(String srcPath, String dstPath) {
       // 源路径必须存在
       Node source = locateNode(srcPath);  // 调用核心方法
       if (source == null) return;

       // 目标父目录必须存在
       Directory dstParent = getParentDirectory(dstPath);  // 调用核心方法
       if (dstParent == null) return;

       // 不允许替换根目录
       String normalized = PathUtil.normalize(dstPath);
       if (normalized == null || normalized.equals("/")) return;

       String dstName = getBaseName(dstPath);  // 调用核心方法

       // 创建链接(TreeMap会自动覆盖已有节点)
       dstParent.putChild(dstName, new Link(dstName, source));
   }
   ```

#### 测试要求
- 测试MKDIR的覆盖语义(文件/链接被替换,目录保持不变)
- 测试LINK创建链接到文件和目录
- 测试LINK的覆盖语义

---

### 成员4 (hly)：TOUCH命令 + RM命令

**依赖**：自己的核心方法

#### 任务清单

1. **升级TOUCH命令** (支持覆盖语义)
   ```java
   public void touch(String absPath, int size) {
       if (size < 0) return;

       Directory parent = getParentDirectory(absPath);  // 调用核心方法
       if (parent == null) return;

       String normalized = PathUtil.normalize(absPath);
       if (normalized == null || normalized.equals("/")) return;

       String name = getBaseName(absPath);  // 调用核心方法

       // 检查是否已存在文件
       Node existing = parent.getChild(name);
       if (existing != null && existing.isFile()) {
           // 已存在文件,覆盖大小
           ((File)existing).setSize(size);
       } else {
           // 创建新文件(TreeMap会自动覆盖目录/链接)
           parent.putChild(name, new File(name, size));
       }
   }
   ```

2. **实现RM命令**
   ```java
   public void rm(String absPath) {
       String normalized = PathUtil.normalize(absPath);
       if (normalized == null || normalized.equals("/")) return;

       Node node = locateNode(absPath);  // 调用核心方法
       if (node == null) return;

       Directory parent = getParentDirectory(absPath);  // 调用核心方法
       if (parent == null) return;

       // 如果是目录,检查是否为空
       if (node.isDirectory()) {
           Directory dir = (Directory) node;
           if (!dir.listChildren().isEmpty()) {
               return;  // 非空目录,忽略
           }
       }

       // 删除节点(文件/链接/空目录)
       String name = getBaseName(absPath);  // 调用核心方法
       parent.removeChild(name);
   }
   ```

3. **集成测试**
   - 使用需求文档中的标准输入输出示例测试
   - 协调所有成员进行联调

#### 测试要求
- 测试TOUCH的覆盖语义
- 测试RM对文件、链接、空目录、非空目录的处理
- 集成测试所有命令的协作

---

## 协作接口约定

| 组件 | 提供者 | 方法签名 | 说明 |
|------|--------|---------|------|
| Link | 成员1 | `Link(String name, Node target)` | 链接类 |
| Link.getTarget() | 成员1 | `Node getTarget()` | 获取目标 |
| SizeContext | 成员1 | `isVisited(Node)`, `markVisited(Node)` | 防重复计数 |
| PathUtil.normalize() | 成员3 | `String normalize(String)` | 路径规范化 |
| PathUtil辅助方法 | 成员3 | `split()`, `getParentPath()`, `getBaseName()` | 路径解析 |
| MemFs核心方法 | 成员4 | `locateNode()`, `getParentDirectory()`, `resolveLink()` | **所有人必须使用** |

---

## 完成优先级

```
第1阶段(5.28-5.30):
  ├─ 成员1: Node体系 + Link + SizeContext ✅
  ├─ 成员3: PathUtil.normalize() + 辅助方法 ✅
  └─ 各自完成单元测试

第2阶段(5.31-6.2):
  ├─ 成员4: MemFs核心方法 + Main类 ✅
  └─ 完成单元测试

第3阶段(6.3-6.6):
  ├─ 成员1: INFO命令 ✅
  ├─ 成员2: LS + FIND命令 ✅
  ├─ 成员3: MKDIR + LINK命令 ✅
  ├─ 成员4: TOUCH + RM命令 ✅
  └─ 各自完成单元测试

第4阶段(6.6):
  └─ 成员4: 集成测试 + 联调 + 打包提交

第5阶段（6.7-6.14）：debug
```

---

## 关键注意事项

1. **⭐ 禁止重复实现**：所有命令必须调用MemFs的核心私有方法,不允许自己实现路径解析/节点定位逻辑
2. **及时测试**：每个成员完成自己的部分后立即测试,不等集成
3. **覆盖语义**：统一使用`putChild`实现覆盖,TreeMap自动替换
4. **防重复**：
   - INFO使用SizeContext防止重复计数
   - FIND使用Set<Directory>防止重复展开
5. **边界情况**：所有非法操作静默忽略,不输出错误信息
6. **File需要setSize**：成员1需要为File类添加`setSize(int size)`方法,供TOUCH覆盖文件大小使用

---
